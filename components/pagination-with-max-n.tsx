/**
 * Creates our pagination using the base pagination component and also allows the changing of the search parameter for
 * the page.
 */

export default function PaginationToN({ nPages } : { nPages : number } ) {
    return (
        <>
        </>
    )
}